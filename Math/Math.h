#include "../Base/base.h"

namespace calg{
    extern "C" EXTERN_API inline ll max(ll a, ll b);
    extern "C" EXTERN_API inline ll min(ll a, ll b);
    extern "C" EXTERN_API inline ll abs(ll x);
    extern "C" EXTERN_API inline ll pow(ll x, ll t);
    extern "C" EXTERN_API inline ll gcd(ll a, ll b);
}
